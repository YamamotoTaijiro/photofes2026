# 写真展AR解説 — 仕様書

このアプリの構成・機能・運用ルールをまとめた資料です。導入や写真の追加・入れ替え手順は
[導入手順.md](導入手順.md) を参照してください。

## 概要

展示写真にスマホのカメラをかざすと、その写真のタイトル・解説文・撮影者名が画面下に表示される
Web ARガイドです。アプリのインストールは不要で、ブラウザでURLを開くだけで動作します。

- 公開URL: https://photofes2026.gokalab.jp/
- GitHubリポジトリ: `YamamotoTaijiro/photofes2026`（GitHub Pagesで公開）

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | 本番公開しているARガイド本体。単一HTMLファイルで、ビルド不要 |
| `index-vX.Y.html` | 過去バージョンのバックアップ（`index.html`を置き換える前に必ず退避） |
| `targets.mind` | 展示写真をMindARが認識できる形式にコンパイルしたマーカーファイル |
| `targets-large.mind` | 画像サイズ縮小前の旧`targets.mind`。認識精度が落ちた場合の保険としてローカルのみ保持（Gitには含めていない） |
| `CNAME` | GitHub Pagesの独自ドメイン設定(`photofes2026.gokalab.jp`) |
| `導入手順.md` | 写真の差し替え・タイトル編集・公開手順など、運用者向けの手引き |
| `counter-worker/` | 写真ごとの認識回数を記録するCloudflare Worker + D1のソース(下記「認識回数の記録」参照) |

## 技術構成

- **AR認識**: [MindAR](https://hiukim.github.io/mind-ar-js-doc/) + [A-Frame](https://aframe.io/)（どちらもCDN読み込み）
- **写真の枚数**: 48枚（`panel0`〜`panel47`の説明パネルと`target0`〜`target47`のARターゲットが対応。`index.html`内`WORK_COUNT`で管理）
- **ホスティング**: GitHub Pages（独自ドメイン`photofes2026.gokalab.jp`）
- **認識ログの保存**: Cloudflare Workers + D1（無料枠で運用）

## 主な機能

1. **写真認識と解説表示**
   写真を認識すると、対応するパネルが画面下からスライドして表示されます。写真から外れても
   すぐには消えず、次に別の写真（または同じ写真の再認識）が確定するまで表示を維持します。

2. **複数認識時の案内**
   2枚以上の写真が同時にカメラに映ると、どちらの解説か判断できなくなるため、説明の切り替えを
   一旦止め、「複数の写真を認識しています。1枚だけになるようカメラを近づけてください」という
   案内を表示します。

3. **ピンチズーム**
   展示写真まで距離があり複数枚が画角に入ってしまう場合に備え、2本指のピンチイン/ピンチアウトで
   ズームできます。画面表示だけを拡大するのではなく、`getUserMedia`を横取りしてカメラ映像自体を
   裏側で切り出し・拡大してからMindARに渡しているため、認識の絞り込みにも実際に効果があります。

4. **バージョン表示**
   スタート画面右下に小さく `v2.3` のようなバージョン番号を表示しています。GitHub Pagesの反映
   確認用で、`index.html`を更新するたびに番号を上げています。

5. **認識回数の記録**
   どの写真が多く見られたかを、Cloudflare Workers + D1で記録しています。
   - Worker URL: `https://photofes2026-counter.taijiro.workers.dev`
   - `POST /track` — 写真を認識するたびに`navigator.sendBeacon()`で送信(失敗してもAR体験には影響しません)
   - `GET /stats` — 写真ごとの認識回数を多い順に表示(ブラウザで直接開けます)
   - `GET /log` — 直近500件の生ログ(日時・写真・IPアドレス・User-Agent)。テストで記録された
     データを見分けて削除したい場合などに使用
   - データの削除は `counter-worker/` 内で `npx wrangler d1 execute photofes2026-counter --remote --command="DELETE FROM views WHERE ..."` を実行

## 運用ルール

- **バージョン管理**: `index.html`を変更したら、右下の`#versionTag`を必ず上げる。`targets.mind`を
  更新した場合は、`imageTargetSrc`の`?v=...`もあわせて上げる(古いキャッシュが残るのを防ぐため)
- **本番差し替え**: 新しいバージョンを試して問題なければ`index.html`に反映するが、その前に現行の
  `index.html`を`index-vX.Y.html`として必ず退避してから置き換える
- **写真の追加・削除**: パネル(`panelN`)とターゲット(`targetN`)は必ず同じ枚数を保ち、`WORK_COUNT`も
  実際の枚数に合わせて変更する

## 既知の注意点

- Google Pixel 10 Pro(Android)では、iPhoneと比べて認識がやや遅く感じられることがある。ズーム
  機能が原因というより、その端末でのMindAR自体の処理速度による可能性が高く、未解決
- `targets.mind`は約34MB(旧バージョンは約55MB)あり、初回読み込みに時間がかかるため、スタート
  画面表示中から裏でダウンロードを開始する対策を入れている
