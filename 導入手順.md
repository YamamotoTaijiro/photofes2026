# 写真展AR解説 — 導入手順

## 全体の流れ

1. 展示する写真(JPEG/PNG)を用意する
2. 写真を「マーカー」として認識できるファイル(`targets.mind`)に変換する
3. `ar_template_production.html` の中身(タイトル・解説文)を書き換える
4. HTMLと`targets.mind`を同じ場所に置いて、HTTPS対応のサーバーにアップロードする
5. スマホのブラウザ(Safari / Chrome)でそのURLを開く → 完成

---

## 手順1: 写真をマーカーに変換する

MindARが提供している無料のオンラインツールを使います。

- URL: https://hiukim.github.io/mind-ar-js-doc/tools/compile

1. ページ内の「Choose File」から、展示する写真を**まとめて**アップロード
   (この順番が `targetIndex: 0, 1, 2...` の並び順になります)
2. 「Start」をクリックすると自動で解析が始まります
3. 完了すると `targets.mind` というファイルがダウンロードされます

**認識精度を上げるコツ**
- 写真同士があまり似た構図・色合いにならないようにする
- 模様や輪郭がはっきりした写真の方が認識しやすい(単色に近い、ぼかしの強い写真は認識しづらい)
- 展示前に実際の照明・展示距離でテストする

---

## 手順2: HTMLを編集する

`ar_template_production.html` を開いて、以下を写真の枚数分書き換えます。

- `<div id="panel0">` 〜 のタイトル・解説文
- 写真が4枚以上ある場合は、`panel3`、`target3` ... と同じパターンで追加してください

---

## 手順3: 公開する(ホスティング)

カメラを使うため **HTTPS** 環境が必須です。無料で手軽なのは以下です。

- **GitHub Pages**(無料・独自ドメインも可)
- **Glitch**(無料・すぐ公開できる)
- **Netlify / Vercel**(無料枠あり)

`ar_template_production.html` と `targets.mind` を同じフォルダにアップロードするだけで動きます。

---

## 手順4: 会場での案内

- 会場入口に一つだけ大きめのQRコード(または短縮URL)を掲示し、そこからサイトへ誘導
  → 「各写真にQR」ではなく「入口に一つだけ」なので景観を損ないません
- サイトを開いたら「カメラを起動する」をタップしてもらい、あとは写真にかざすだけです

---

## 会場でうまく動かないときのチェックリスト

- HTTPSで公開しているか(HTTPだとカメラが起動しません)
- 照明が暗すぎないか(展示照明が暗い場合は認識率が落ちます)
- 写真同士が似すぎていないか(誤認識の原因になります)
- スマホの画面から写真までの距離(20〜60cm程度が目安)
